/**
 * Опознание человека в списках мини-аппа.
 *
 * Само правило — в `@planer/shared/person-identity`: одна палитра на оба
 * фронта, потому что один и тот же человек не может читаться разным цветом в
 * мини-аппе и в консоли. Здесь остался реэкспорт, чтобы у импортов не поменялся
 * путь.
 */
export { initialsOf, personPalette, type PersonPalette } from "@planer/shared";
