{
  "name": "planer-bot",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.22.2" },
  "workspaces": ["shared", "server", "miniapp", "admin"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p shared/tsconfig.json && tsc -p server/tsconfig.json && tsc -p admin/tsconfig.json && tsc -p miniapp/tsconfig.json"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
